const hardware = ['mouse', 'keyboard', 'screen',]

const newHardware = hardware.slice(0,2);

console.log(newHardware);


const myObject = {
    name: "Maciek",
    age: 35
}


hardware.push(myObject);

console.log(hardware);

const myText = "tu przykładowy string"
console.log(myText.slice(0,7));